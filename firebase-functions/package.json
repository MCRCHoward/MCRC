{
  "name": "mcrc-firebase-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "lint": "tsc --noEmit",
    "test": "npm run lint",
    "deploy": "npm run lint && npm run test && npm run build && firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "rimraf": "^6.0.1",
    "typescript": "^5.3.3"
  }
}
