// src/components/RichText/index.tsx (Payload removed)
import type { HTMLAttributes } from 'react'

type Props = {
  data?: unknown | null
  enableGutter?: boolean
  enableProse?: boolean
} & HTMLAttributes<HTMLDivElement>

export default function RichText(_props: Props) {
  return null
}
